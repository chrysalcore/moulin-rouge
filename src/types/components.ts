// Tipos para componentes
export type Brief = {
    [key: string]: string
}

export type Header = {
    title: string
    desc: string
}

export type SectionType = 'about' | 'dishes' | 'contact' | 'reservation'
